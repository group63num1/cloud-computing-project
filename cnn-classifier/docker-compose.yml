version: '3.8'

services:
  app:
    image: ${IMAGE_NAME:-cnn-classifier:local}
    build: .
    ports:
      - "${HOST_PORT:-8000}:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
